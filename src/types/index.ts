export type pokemonListType ={
  name: string, 
  url: string
}

export type User = {
  id: number,
  name: string
}

export type Product = {
  id: number;
  name: string;
  price: number;
  category: string;
  rating: number;
};
